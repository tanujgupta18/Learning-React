// Passing Multiple Props

const Case4 = ({ name, age, location }) => {
  return (
    <div>
      <h2>{name}</h2>
      <p>Age: {age}</p>
      <p>Location: {location}</p>
    </div>
  );
};

export default Case4;
